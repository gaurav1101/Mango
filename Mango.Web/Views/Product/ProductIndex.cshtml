
@model IEnumerable<Mango.Web.Models.Dto.ProductDto>

<div class="card shadow border-0 mt-4">
	<div class="card-header bg-secondary bg-gradient ml-0 py-3">
		<div class="row">
			<div class="col-12 text-center">
				<h1 class="text-white">Products List</h1>
			</div>
		</div>
	</div>
	<div class="card-body p-4">
		<div class="row pb-3">
			<div class="col-6">
			</div>
			<div class="col-6 text-end">
				<a asp-action="CreateProduct" class="btn btn-outline-primary"><i class="bi bi-plus-square"></i> Create New Product</a>

			</div>
		</div>
		<table class="table">
			<thead>
				<tr>
					<th>
						Product Name
					</th>
					<th>
						Category
					</th>
					<th>
						Price
					</th>
					
					<th></th>
				</tr>
			</thead>
			<tbody>
				@foreach (var item in Model)
				{
					<tr>
						<td>
							@item.Name
						</td>
						<td>
							@item.CategoryName
						</td>
						<td>
							@((item.Price).ToString("c"))
						</td>

						<td class="text-end">
							<a class="btn btn-success" asp-controller="Product" asp-action="UpdateProduct" asp-route-productId="@item.ProductId">
								<i class="bi bi-pencil-square"></i>
							</a>
							&nbsp;

							<a class="btn btn-danger" asp-controller="Product" asp-action="RemoveProduct" asp-route-productId="@item.ProductId">
								<i class="bi bi-trash"></i>
							</a>
							
							
						</td>
							
					</tr>
				}
			</tbody>
		</table>

	</div>

</div>


@section Scripts {
	<partial name="_ValidationScriptsPartial" />
}